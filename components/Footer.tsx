import styles from '../styles/Footer.module.css';

const Footer = () => (
  <div className={styles.container}>
    <h3 className={styles.title}>Next Cafe</h3>
    <p className={styles.description}>cafe for your next visit</p>
  </div>
);

export default Footer;
